{
  "name": "lindemannrock-base",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd src/web/assets && npm run build",
    "build:analytics": "cd src/web/assets && npm run build:analytics",
    "build:components": "cd src/web/assets && npm run build:components",
    "build:install": "cd src/web/assets && npm run build:install"
  },
  "license": "Proprietary"
}
